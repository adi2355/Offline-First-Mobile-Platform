# Screenshots

Capture a small, high-signal set:

- dashboard or primary overview with local data visible
- device connection or pairing state
- health view populated from local projections
- offline mutation with pending / synced state transition

Do not flood the repository with generic screen captures.
